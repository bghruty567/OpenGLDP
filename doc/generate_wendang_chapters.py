from __future__ import annotations

import re
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "wendang"

FORBIDDEN = re.compile(r"最小二乘|WLS|AWLS|least\s*squares", re.I)


def md_table(headers: list[str], rows: list[list[str]]) -> str:
    lines = [
        "| " + " | ".join(headers) + " |",
        "| " + " | ".join(["---"] * len(headers)) + " |",
    ]
    lines.extend("| " + " | ".join(row) + " |" for row in rows)
    return "\n".join(lines)


ANALYTIC_SCALAR = [
    ["SampleStructGrid", "三维线性标量场", "2.82038e-07"],
    ["hexa", "三维线性标量场", "1.57998e-07"],
    ["1_0", "曲面切向线性标量场", "1.68088e-07"],
]

ANALYTIC_VECTOR = [
    ["SampleStructGrid", "三维线性向量场", "3.30256e-07"],
    ["hexa", "三维线性向量场", "2.36149e-07"],
    ["1_0", "曲面切向线性向量场", "1.32859e-07"],
]

VTK_POINT = [
    ["SampleStructGrid", "scalars", "8.22437e-08"],
    ["hexa", "scalars", "6.27285e-08"],
    ["1_0", "RF", "5.63477e-08"],
]

VTK_CELL = [
    ["SampleStructGrid", "scalars", "7.62786e-07"],
    ["limb", "chem_0", "3.89516e-07"],
    ["1_0", "S_Mises", "7.77806e-08"],
]

TIME_STRUCT = [
    ["timing_struct_20x20x20", "8000 / 6859", "16", "2.7573", "0.9036", "0.025272"],
    ["timing_struct_32x32x32", "32768 / 29791", "16", "3.5564", "1.4336", "0.043732"],
    ["timing_struct_48x48x48", "110592 / 103823", "16", "8.2716", "4.0996", "0.107692"],
]

TIME_UHEX = [
    ["timing_uhex_20x20x20", "8000 / 6859", "16", "163.799", "2.8527", "0.623896"],
    ["timing_uhex_32x32x32", "32768 / 29791", "16", "708.650", "5.3835", "2.53781"],
    ["timing_uhex_48x48x48", "110592 / 103823", "16", "3349.36", "44.8039", "7.91991"],
]

OPTIMIZATION = [
    ["POINT", "0.296642", "0.180858", "0.610772"],
    ["CELL", "0.294558", "0.121921", "0.414806"],
]


FILES: dict[str, str] = {
    "00_摘要.md": f"""# 摘要

计算机辅助工程（CAE）后处理阶段需要对求解器输出的网格、节点场、单元场和派生物理量进行进一步处理，才能支撑后续的颜色映射、等值面观察、局部异常分析和工程判断。随着仿真模型规模增大，梯度计算、局部平滑和结果导出等预处理环节的效率和一致性逐渐成为影响后处理体验的重要因素。本文围绕这一问题，设计并实现了一套基于 OpenGL 的 CAE 数据预处理原型系统。

系统采用 C++17、VTK、Qt 和 OpenGL Compute Shader 进行开发。VTK 负责数据读写、结果对照和可视化观察入口；Qt 提供基础图形界面；OpenGL 计算着色器承担核心预处理计算。系统首先将 VTK 数据转换为统一内部数据对象，保存点坐标、单元连接、单元类型、点数据、单元数据以及邻域关系；随后由门面层根据网格类型和字段关联方式调度不同算法；最终将计算结果写回内部字段数组，并支持重新导出为 VTK 文件。

在梯度计算方面，本文将方法口径收敛为两条主线：规则网格采用有限差分法，非结构化网格采用基于形函数导数的方法。非结构化网格路径支持一阶三角形、四边形、四面体和六面体四类单元。实验部分按照解析场验证、与 VTK 结果一致性对比、与 VTK 时间对比三类组织。解析场验证表明，系统在 SampleStructGrid、hexa 和 1_0 数据集上的线性标量场与线性向量场平均相对误差均达到 1e-7 量级；真实字段对比表明，系统结果与 vtkGradientFilter 保持较高一致性；时间实验表明，在统一生成的规则网格族和非结构化六面体网格族上，系统总时间和 GPU 计算时间均明显低于当前测试条件下的 VTK 并行时间。

在数据优化方面，本文不将其表述为通用降噪模块，而是收敛为“面向一类局部随机高频数值扰动的抑制与边缘保持处理”。CAE 仿真数据可能受到离散化误差、迭代收敛误差与舍入误差影响，其中一类扰动表现为局部随机高频波动。本文采用高斯扰动作为代理模型，在 ShipHull_0 数据集上构造干净场并叠加扰动，通过优化前后误差对比验证模块效果。实验结果显示，点数据平均相对误差由 0.296642 降至 0.180858，单元数据平均相对误差由 0.294558 降至 0.121921，说明该模块对目标扰动具有稳定抑制作用。

本文的主要贡献在于：构建了规则网格与非结构化网格统一的数据预处理流程；实现了基于 OpenGL Compute Shader 的梯度计算与数据优化模块；建立了与解析真值、VTK 输出和 VTK 时间对照相结合的实验体系；形成了可运行、可导出、可复现的本科毕业设计原型系统。本文工作不以替代完整 CAE 后处理软件为目标，而是为后处理中的数据准备与 GPU 并行计算提供了一个边界清晰的研究与实践案例。

**关键词**：CAE 后处理；OpenGL；Compute Shader；梯度计算；形函数导数；数据优化
""",
    "01_绪论.md": """# 第一章 绪论

## 1.1 研究背景

计算机辅助工程（Computer Aided Engineering，CAE）已经成为现代工程设计和产品分析中的重要技术手段。工程人员可以通过有限元分析、流体分析、热分析和多物理场仿真，在产品制造之前对结构强度、温度分布、流场特征和局部风险进行评估。CAE 求解器输出的结果通常并不是直接面向用户理解的最终信息，而是大量离散网格、节点场、单元场和时间步数据。为了从这些数据中得到工程判断，需要经过后处理阶段的进一步分析与可视化。

后处理阶段不仅包含图像显示，也包含大量数据预处理操作。例如，在结构分析中，工程人员可能需要观察应力场梯度来判断应力集中区域；在流体分析中，速度场梯度、涡量和压力变化能够帮助识别流动特征；在热分析中，温度梯度能够反映热传导路径和局部突变。除此之外，仿真数据还可能受到离散化误差、迭代收敛误差和舍入误差影响，导致局部字段出现高频波动。若不进行适当处理，后续颜色映射和局部观察可能被噪声或局部异常扰动干扰。

VTK 和 ParaView 是科学可视化领域常用的开源工具。VTK 提供了丰富的数据模型、读写接口和过滤器，ParaView 则在 VTK 基础上提供了更完整的交互式可视化环境。对于工程应用而言，这些工具具有成熟、稳定、功能丰富的优势。但是从本科毕业设计和底层技术研究角度看，仅调用现有过滤器并不能充分体现数据结构设计、并行计算组织和算法实现能力。因此，本文在保留 VTK 数据读写和参考对照能力的基础上，尝试将部分 CAE 数据预处理计算迁移到 OpenGL Compute Shader 中实现。

OpenGL 通常被视为图形渲染接口，但现代 OpenGL 提供的 Compute Shader 已经具备通用并行计算能力。通过 GPU 缓冲区保存点坐标、单元连接、字段数组和邻域索引，程序可以让每个线程负责一个点或一个单元的局部计算，从而利用 GPU 的并行吞吐能力。对于梯度计算和局部滤波这类具有较强数据并行特征的任务，OpenGL 计算管线具有明确的实践价值。

## 1.2 研究问题

本课题关注的问题可以概括为：如何在 CAE 后处理数据预处理场景中，构建一条从 VTK 数据输入到 GPU 计算再到结果导出的完整原型流程。该问题并不是要开发完整 CAE 后处理平台，而是聚焦于后处理前端的几个核心数据准备环节。

首先，CAE 数据存在多种网格类型。规则网格具有明确的逻辑维度和相邻关系，适合使用有限差分进行梯度计算；非结构化网格通过显式单元连接描述拓扑，适合从单元形函数和几何映射角度处理梯度。若系统内部直接依赖 VTK 对象进行所有计算，GPU 端访问会比较复杂；若重新设计内部数据表示，又需要保证点数据、单元数据和单元连接能够正确保留。因此，统一内部数据结构是系统设计的基础问题。

其次，梯度计算既要考虑算法正确性，又要考虑工程一致性。解析场实验能够提供真值，用于验证算法本身是否正确；真实字段与 vtkGradientFilter 的对比则能够说明系统输出在工程软件语境下是否一致。两类实验的意义不同，不能互相替代。本文将实验明确分为解析场验证、VTK 结果一致性对比和时间对比三部分，避免用单一指标解释所有问题。

再次，数据优化模块必须有明确边界。CAE 仿真结果中的扰动来源复杂，不同噪声模型需要不同处理方法。本文不将数据优化模块描述为通用降噪器，而是面向一类局部随机高频数值扰动，采用高斯扰动作为代理模型进行验证。这样的表述更符合当前系统实现和实验数据，也更便于答辩时说明适用范围。

## 1.3 研究目标

本文研究目标包括以下几个方面。

第一，设计并实现统一内部数据表示。系统需要从 VTK 文件中读取点坐标、单元连接、单元类型、点数据和单元数据，并转换为 GPU 友好的扁平数组结构。该结构既要支持规则网格，也要支持非结构化网格，还要能在计算结束后重新导出为 VTK 数据。

第二，实现基于 OpenGL Compute Shader 的梯度计算模块。规则网格采用有限差分法，根据网格维度和相邻采样点计算梯度；非结构化网格采用基于形函数导数的方法，支持一阶三角形、四边形、四面体和六面体单元。系统应支持点数据和单元数据两类字段。

第三，实现面向局部随机高频数值扰动的数据优化模块。该模块以图双边滤波和多尺度融合为基础，在保留主要结构和边缘特征的前提下抑制局部高频波动。实验中使用高斯扰动代理局部随机扰动，并比较优化前后的平均相对误差。

第四，建立可复现实验体系。论文实验应包括解析场验证、与 VTK 结果一致性对比、与 VTK 时间对比以及数据优化效果验证。每组实验都应明确数据集、字段、评价指标和结论边界。

## 1.4 本文主要工作

本文完成的主要工作如下。

1. 构建了基于 VTK、Qt 和 OpenGL 的 CAE 数据预处理原型系统，实现了数据读入、内部转换、GPU 计算、结果写回和 VTK 导出流程。
2. 设计了统一内部数据结构，用于保存规则网格和非结构化网格中的点、单元、字段和邻域信息。
3. 实现了规则网格有限差分梯度计算和非结构化网格形函数导数梯度计算。
4. 实现了面向局部随机高频扰动的数据优化流程，并通过高斯扰动代理实验验证效果。
5. 根据 `毕设进展.docx` 中整理的实验口径和数据，形成了解析场、VTK 一致性、时间性能和数据优化四类实验结果。

## 1.5 论文结构

全文共分为六章。第一章介绍研究背景、问题、目标和主要工作。第二章介绍相关技术与理论基础，包括 VTK 数据模型、OpenGL Compute Shader、有限差分、形函数导数和图双边滤波。第三章介绍系统需求分析与总体设计。第四章介绍核心算法设计与实现。第五章给出实验设计和结果分析。第六章总结全文工作并提出后续展望。
""",
    "02_相关技术与理论基础.md": """# 第二章 相关技术与理论基础

## 2.1 CAE 后处理数据模型

CAE 后处理数据通常由几何网格和物理字段组成。几何网格描述仿真区域的离散结构，物理字段描述求解结果在网格上的分布。常见字段包括位移、应力、温度、压力、速度等。根据字段附着位置不同，可分为点数据和单元数据。点数据定义在节点上，适合表现节点处物理量；单元数据定义在单元上，通常表示单元内部的平均量或积分点恢复量。

规则网格和非结构化网格在数据组织方式上存在明显差异。规则网格具有明确的逻辑维度，可以通过三维索引直接找到相邻点，适合进行有限差分计算。非结构化网格没有统一的逻辑索引，而是通过单元连接显式描述拓扑关系。非结构化网格能够适应复杂几何边界，因此在工程仿真中使用广泛，但它的梯度计算、邻域构建和数据访问也更加复杂。

本文系统需要同时处理规则网格和非结构化网格，因此内部数据结构不能只服务于某一种网格类型。系统将点坐标、单元连接、单元类型、点数据、单元数据和邻域关系转换为统一扁平数组。这样既方便 CPU 端管理，也方便 GPU 端通过索引直接访问。

## 2.2 VTK 与 ParaView

VTK 是科学可视化领域常用的开源库，提供了丰富的数据结构、读写接口、过滤器和渲染支持。ParaView 是基于 VTK 的可视化应用，适合对大规模科学数据进行交互式观察。本文使用 VTK 的主要原因有三个：第一，VTK 支持常见科学数据格式，便于读取现有测试数据；第二，VTK 提供 vtkGradientFilter，可作为真实字段工程一致性对照；第三，计算结果可以导出为 VTK 文件，再在 ParaView 中观察。

需要强调的是，本文没有把 VTK 作为核心算法的简单封装。系统使用 VTK 进行数据输入、输出和参考对比，而核心梯度计算和数据优化由 OpenGL Compute Shader 实现。这样的设计既利用了 VTK 成熟的数据生态，也保留了本文对 GPU 计算流程的研究价值。

## 2.3 OpenGL Compute Shader

OpenGL Compute Shader 是现代 OpenGL 中用于通用并行计算的着色器阶段。与顶点着色器、片元着色器不同，Compute Shader 不直接依赖图形图元输出，而是通过工作组和全局线程编号执行计算任务。程序可以将输入数据放入 GPU 缓冲区，让着色器按索引读取数据、执行计算并写入输出缓冲区。

在本文系统中，点坐标、字段值、单元连接和结果数组均以连续数组形式传输到 GPU。对于规则网格梯度计算，每个线程处理一个点，根据点的逻辑索引寻找相邻采样点。对于非结构化网格梯度计算，每个线程可以围绕一个点或一个单元，根据单元连接和形函数导数计算梯度贡献。对于数据优化，每个线程处理一个字段采样位置，根据邻域索引进行局部加权计算。

OpenGL 计算方式的优势在于并行度高、与图形渲染生态兼容、部署相对轻量。限制在于需要程序员自行管理缓冲区、数据对齐、同步和错误检查。本文系统通过计算引擎类封装着色器编译、缓冲上传、派发和结果回读，使上层门面接口不必直接处理底层 OpenGL 细节。

## 2.4 规则网格有限差分

有限差分法是规则网格上计算梯度的常用方法。对于标量场，梯度表示该标量沿空间方向变化的快慢。规则网格中的采样点排列有序，因此可以通过相邻点的值构造差分近似。内部点通常使用中心差分，边界点则使用前向差分或后向差分。

有限差分法适合本文中的规则网格路径，原因在于它充分利用规则网格的逻辑结构，不需要额外构建复杂拓扑。GPU 实现时，线程根据点索引恢复其在三维网格中的位置，再判断该点是否位于边界。由于不同点之间计算基本独立，该方法具有良好的并行性。

## 2.5 非结构化网格形函数导数

非结构化网格没有规则索引，因此不能直接套用规则网格有限差分。本文采用基于形函数导数的方法。有限元单元内部通常通过形函数描述场变量随局部坐标的变化关系。对于一阶三角形、四边形、四面体和六面体单元，形函数具有明确的局部表达式。通过单元几何映射，可以把局部坐标下的导数转换为物理空间中的导数。

该方法与单元结构密切相关，适合解释本文当前支持的四类单元。对于点数据，可以根据相邻单元对节点梯度的贡献进行组织；对于单元数据，可以结合单元中心和相邻关系计算派生结果。与规则网格有限差分相比，形函数导数路径更复杂，但它更符合非结构化网格的有限元语义。

## 2.6 图双边滤波与多尺度融合

数据优化模块面向一类局部随机高频数值扰动。双边滤波的核心思想是同时考虑空间距离和数值差异：空间上更近的样本权重更高，数值上更接近的样本权重更高。这样可以在平滑局部波动的同时，减少跨越明显边缘的过度平均。

在网格数据中，空间关系由邻域图描述。系统可以对每个点或单元遍历其邻域，根据几何距离和字段差异计算权重，再得到平滑结果。多尺度融合则进一步构造不同平滑程度的尺度层，通过基础层和细节层组合得到最终优化字段。本文实验使用高斯扰动作为代理模型，验证该模块对局部随机高频扰动的抑制效果。

## 2.7 评价指标

本文主要使用平均相对误差、系统总时间、GPU 计算时间、VTK 并行时间和改进比作为指标。解析场验证中的平均相对误差用于衡量系统结果与解析真值的接近程度。VTK 一致性实验中的平均相对误差用于衡量系统输出与 vtkGradientFilter 的一致程度。时间实验中的系统总时间包括数据准备、GPU 计算和必要的回读开销，比单独 GPU 时间更接近真实使用成本。数据优化实验中的改进比表示优化后误差相对输入误差的比例，数值越小表示误差降低越明显。
""",
    "03_系统需求分析与总体设计.md": """# 第三章 系统需求分析与总体设计

## 3.1 需求分析

本文系统面向 CAE 后处理阶段的数据预处理任务，核心需求包括数据读入、数据转换、梯度计算、数据优化、结果导出和基础交互。系统不实现完整求解器，也不实现完整商业 CAE 后处理平台，而是聚焦于后处理前端的数据准备过程。

功能需求主要包括五类。第一，系统应能够读取 VTK 数据文件，获得点坐标、单元连接、点数据和单元数据。第二，系统应能够将不同网格类型转换为统一内部表示，供 GPU 计算使用。第三，系统应根据网格类型执行梯度计算：规则网格采用有限差分法，非结构化网格采用形函数导数法。第四，系统应提供面向局部随机高频扰动的数据优化功能。第五，系统应能够将处理结果写回数据对象并导出为 VTK 文件。

非功能需求包括可复现性、可扩展性和性能。可复现性要求实验程序与 GUI 使用同一底层接口，避免演示结果和实验结果来自不同代码路径。可扩展性要求数据结构能够继续支持更多字段和更多单元类型。性能要求核心计算尽可能在 GPU 上并行执行，并在实验中与 VTK 并行时间进行对照。

## 3.2 总体架构

系统总体流程为：VTK 文件输入 -> VTK 数据转换 -> 内部数据对象 -> OpenGL 计算引擎 -> 结果字段写回 -> VTK 导出或 GUI 显示。该流程将数据读写、计算执行和结果展示分离开来，使每一层职责相对清晰。

数据输入层负责读取 VTK 文件，并将其转换为 VTK 数据集对象。数据转换层负责提取点坐标、单元连接、单元类型和字段数组，并转换为内部 `DataObject`。门面层 `CAEProcessingFacade` 负责保存数据集记录、选择算法、调用计算引擎和记录时间。计算执行层包括梯度计算引擎和数据优化引擎，负责与 OpenGL 上下文和计算着色器交互。输出层负责把结果写回内部数组并重新导出为 VTK 文件。

这种架构避免了界面层直接操作底层 OpenGL 细节，也避免了测试程序重复实现数据加载和计算流程。GUI、测试程序和批处理实验都可以通过门面层调用同一套核心功能。

## 3.3 内部数据结构设计

内部数据结构的核心目标是兼容规则网格和非结构化网格，并适配 GPU 连续内存访问。系统将点坐标保存为一维浮点数组，每个点连续保存 x、y、z 三个分量。单元连接保存为索引数组，并配合偏移数组确定每个单元包含哪些点。单元类型数组用于标识三角形、四边形、四面体或六面体等类型。

字段数组按点数据和单元数据分类保存。每个字段包含字段名、分量数、所属类型和扁平化数据。这样可以支持标量场和向量场，也便于梯度结果作为新字段写回。规则网格额外保存维度信息，便于有限差分计算恢复三维逻辑索引。

邻域关系用于数据优化和部分非结构化处理。系统采用偏移数组加索引数组的方式组织邻域。对于第 i 个点或单元，其邻居索引范围由 offsets[i] 到 offsets[i+1] 确定。该结构紧凑、连续，适合上传到 GPU 缓冲区。

## 3.4 模块划分

系统主要模块包括 `VTKDataConverter`、`DataObject`、`CAEProcessingFacade`、`GLGradientEngine`、`GLFilterEngine`、Qt GUI 和测试程序。

`VTKDataConverter` 负责 VTK 数据与内部数据之间的双向转换。它既要保证字段数据不丢失，也要保证单元连接和类型能够正确恢复。`DataObject` 是算法层真正使用的数据对象，负责保存点、单元、字段和邻域。`CAEProcessingFacade` 是统一门面，负责对外提供加载、字段查询、梯度计算、数据优化和导出接口。

`GLGradientEngine` 封装梯度计算相关 OpenGL 调用，包括规则网格有限差分和非结构化网格形函数导数。`GLFilterEngine` 封装数据优化相关计算，包括图双边滤波和多尺度融合。Qt GUI 提供文件打开、字段选择、计算执行和结果导出入口。测试程序负责批量运行实验并输出表格数据。

## 3.5 算法分派设计

算法分派遵循简单明确的原则：规则网格进入有限差分路径，非结构化网格进入形函数导数路径。这样可以避免在论文和答辩中出现多条并行口径。对于规则网格，系统利用维度信息和相邻索引计算梯度。对于非结构化网格，系统根据单元类型和形函数导数计算梯度。

门面层在收到计算请求后，先检查数据集和字段是否存在，再根据网格类型选择具体方法。计算完成后，结果以新字段形式写回内部数据对象，并记录字段名、源字段、关联方式、输出分量数和时间信息。统一命名和统一写回机制保证 GUI、测试程序和导出结果一致。

## 3.6 GUI 与实验程序设计

GUI 的作用是提供基础交互，而不是承载所有业务逻辑。用户可以通过界面打开 VTK 文件、选择点数据或单元数据、执行梯度计算、执行数据优化并导出结果。界面层只负责收集参数和显示日志，核心计算仍由门面层完成。

实验程序的作用是保证论文数据可复现。梯度实验程序可以运行解析场验证、VTK 一致性对比和时间测试。数据优化实验程序可以构造干净场、叠加高斯扰动、运行优化并统计误差。由于实验程序和 GUI 共用底层接口，论文中的实验结果能够直接反映系统实际能力。
""",
    "04_核心算法设计与实现.md": """# 第四章 核心算法设计与实现

## 4.1 规则网格有限差分梯度

规则网格具有明确的逻辑维度，点可以通过三维索引表示。对于标量场 u，梯度表示 u 在 x、y、z 三个方向上的变化率。内部点可以使用中心差分，即利用正方向和负方向相邻点的场值差近似导数；边界点由于缺少一侧邻点，需要使用前向差分或后向差分。

GPU 实现时，每个线程负责一个网格点。线程根据全局编号计算该点在三维网格中的 i、j、k 坐标，然后判断其是否位于边界。如果位于内部区域，则读取两侧相邻点计算中心差分；如果位于边界，则读取可用方向上的相邻点计算单边差分。对于多分量字段，程序按分量循环计算，每个分量输出三个方向的导数。

有限差分路径的优点是计算结构简单、访问模式规则、并行度高。它适合 SampleStructGrid 等规则网格数据。本文解析场实验中，SampleStructGrid 在线性标量场和线性向量场上的平均相对误差分别为 2.82038e-07 和 3.30256e-07，说明该路径能够正确恢复线性场梯度。

## 4.2 非结构化网格形函数导数梯度

非结构化网格通过单元连接描述拓扑关系，无法直接依赖规则索引计算差分。本文采用基于形函数导数的方法处理非结构化网格。对于一阶三角形、四边形、四面体和六面体单元，单元内部字段可以由节点值和形函数插值表示。形函数在局部坐标下的导数可以根据单元类型确定，再通过几何映射转换到物理空间。

具体来说，单元几何由节点坐标决定。局部坐标到物理坐标的映射可以通过 Jacobian 描述。若已知形函数对局部坐标的导数，则可以通过 Jacobian 的逆变换得到对物理坐标的导数。对于节点字段，系统根据单元连接读取对应节点值，计算单元内的梯度贡献，再组织为点数据输出。对于单元字段，系统结合单元中心和相邻结构得到对应梯度结果。

该方法的优势在于与有限元单元语义一致，能够明确支持一阶三角形、四边形、四面体和六面体。本文在 hexa 和 1_0 数据集上的解析场实验表明，非结构化三维网格和二维曲面网格均能达到 1e-7 量级误差。hexa 在线性标量场和线性向量场上的平均相对误差分别为 1.57998e-07 和 2.36149e-07；1_0 在曲面切向线性标量场和曲面切向线性向量场上的平均相对误差分别为 1.68088e-07 和 1.32859e-07。

## 4.3 点数据与单元数据处理

CAE 数据中的点数据和单元数据具有不同含义。点数据通常表示节点上的物理量，适合直接参与节点梯度恢复。单元数据通常表示单元内部平均量、积分点恢复量或单元级结果，处理时需要围绕单元中心和单元邻接关系组织计算。

系统在字段信息中保存字段关联方式。用户选择字段后，门面层根据字段关联方式和网格类型调用对应计算路径。计算结果也保留相同关联方式，并以新字段形式写回。例如，点数据输入得到点数据梯度结果，单元数据输入得到单元数据梯度结果。这样可以避免字段语义混淆，也便于导出后在 ParaView 中继续观察。

在真实字段与 VTK 一致性实验中，点数据选取 SampleStructGrid/scalars、hexa/scalars 和 1_0/RF，平均相对误差分别为 8.22437e-08、6.27285e-08 和 5.63477e-08。单元数据选取 SampleStructGrid/scalars、limb/chem_0 和 1_0/S_Mises，平均相对误差分别为 7.62786e-07、3.89516e-07 和 7.77806e-08。这说明当前点数据和单元数据路径均能在真实字段上与 VTK 结果保持较高一致性。

## 4.4 数据优化算法

数据优化模块面向一类局部随机高频数值扰动。本文认为，CAE 仿真数据可能受到离散化误差、迭代收敛误差和舍入误差影响，其中部分扰动在结果场上表现为局部随机高频波动。为了验证模块效果，实验中在干净场上叠加高斯扰动，将其作为目标扰动的代理模型。

图双边滤波是该模块的基础。对于每个点或单元，系统遍历其邻域样本，并根据空间距离和字段值差异计算权重。空间距离越近，权重越高；字段值越接近，权重越高。这样可以在局部范围内平滑随机波动，同时降低跨越边缘区域的过度平均。

多尺度融合在单次滤波基础上进一步构造不同平滑程度的尺度层。较浅层保留更多细节，较深层保留更平滑的基础结构。系统通过相邻尺度之间的差异得到细节层，再将基础层和部分细节进行融合，得到最终优化结果。该策略的目标不是简单抹平全部变化，而是在降低局部高频扰动的同时尽量保留主要结构。

## 4.5 OpenGL 实现细节

OpenGL 实现的关键在于缓冲区组织。系统将点坐标、字段值、单元连接、单元类型、邻域偏移和邻域索引分别上传到 GPU 缓冲区。计算着色器通过绑定点访问这些缓冲区，并将结果写入输出缓冲区。计算完成后，CPU 端读取结果并写回内部数据对象。

为了降低上层复杂度，系统将 OpenGL 细节封装在计算引擎类中。引擎负责着色器编译、缓冲区创建、数据上传、计算派发、同步和结果回读。门面层只需要传入内部数组和参数，不需要了解每个缓冲区的底层绑定细节。

## 4.6 结果写回与导出

计算结果以新字段形式写回内部数据对象。字段名中包含源字段和算法含义，便于用户区分原始数据和派生结果。导出时，系统将内部数据对象重新转换为 VTK 数据集，使用户可以在 ParaView 中加载结果文件并观察梯度场或优化场。

这种设计保证了算法结果不仅停留在控制台输出或 CSV 表格中，也能进入常见后处理可视化工具。对本科毕业设计而言，能够从输入数据、核心计算、结果导出到可视化观察形成闭环，是系统完成度的重要体现。
""",
    "05_实验设计与结果分析.md": f"""# 第五章 实验设计与结果分析

## 5.1 实验总体设计

本文实验数据和口径以 `C:/Users/lenovo/Desktop/毕设进展.docx` 为准。实验分为四组：解析场验证实验、与 VTK 结果一致性对比实验、与 VTK 的时间对比实验、数据优化实验。四组实验分别回答不同问题。

解析场验证实验回答“算法本身是否正确”。该实验在可控场函数上计算梯度，并将系统结果与解析真值比较。与 VTK 结果一致性对比实验回答“真实字段上系统输出是否与成熟工具保持一致”。时间对比实验回答“当前 OpenGL 实现相对 VTK 并行实现是否具有效率优势”。数据优化实验回答“面向局部随机高频扰动时，优化模块是否能降低误差”。

## 5.2 解析场验证实验

解析场验证使用 SampleStructGrid、hexa 和 1_0 三个数据集。SampleStructGrid 表示规则网格，用于验证有限差分路径；hexa 表示非结构化三维网格；1_0 表示非结构化二维曲面网格。实验分别构造线性标量场和线性向量场。三维数据集在线性场实验中使用归一化局部坐标构造一次函数；1_0 数据集在曲面局部切向坐标上构造一次函数。

### 5.2.1 线性标量场结果

{md_table(["数据集", "场函数说明", "平均相对误差"], ANALYTIC_SCALAR)}

### 5.2.2 线性向量场结果

{md_table(["数据集", "场函数说明", "平均相对误差"], ANALYTIC_VECTOR)}

从实验结果看，所有数据集的平均相对误差均处于 1e-7 量级。这说明规则网格有限差分路径和非结构化网格形函数导数路径都能够正确恢复线性场梯度。对于毕业论文而言，这组实验是算法正确性的直接证据，应作为梯度模块最重要的实验支撑。

## 5.3 与 VTK 结果一致性对比实验

真实字段通常没有解析真值，因此本文使用 vtkGradientFilter 作为工程参考。需要强调，这组实验说明的是工程输出一致性，而不是数学真值正确性。若系统结果与 VTK 在真实字段上保持接近，说明当前实现能够在常见后处理语境下得到可接受结果。

### 5.3.1 点数据结果

{md_table(["数据集", "字段", "平均相对误差"], VTK_POINT)}

### 5.3.2 单元数据结果

{md_table(["数据集", "字段", "平均相对误差"], VTK_CELL)}

点数据实验中，SampleStructGrid、hexa 和 1_0 的平均相对误差均达到 1e-8 量级。单元数据实验中，三个数据集结果处于 1e-7 到 1e-6 量级。该结果说明系统在真实数据字段上与 VTK 输出保持了较高一致性。论文中应将这部分作为工程可靠性的证据，并与解析场验证形成互补。

## 5.4 与 VTK 的时间对比实验

时间实验不采用异构真实模型，而采用统一生成的同构网格族。这样可以减少模型差异对结论的干扰，使数据规模和时间增长关系更容易解释。实验分别测试规则网格族和非结构化六面体网格族，并记录 VTK 并行线程数、VTK 并行时间、系统总时间和 GPU 计算时间。

### 5.4.1 规则网格时间对比

{md_table(["数据集", "点数/单元数", "VTK 并行线程数", "VTK 并行时间/ms", "系统总时间/ms", "GPU计算时间/ms"], TIME_STRUCT)}

规则网格实验中，随着规模从 20x20x20 增至 48x48x48，系统总时间从 0.9036 ms 增至 4.0996 ms，GPU 计算时间从 0.025272 ms 增至 0.107692 ms。对应 VTK 并行时间从 2.7573 ms 增至 8.2716 ms。系统总时间始终低于 VTK 并行时间，说明规则网格有限差分路径具有较好的执行效率。

### 5.4.2 非结构化网格时间对比

{md_table(["数据集", "点数/单元数", "VTK 并行线程数", "VTK 并行时间/ms", "系统总时间/ms", "GPU计算时间/ms"], TIME_UHEX)}

非结构化网格实验中，系统总时间随规模增长从 2.8527 ms 增至 44.8039 ms，GPU 计算时间从 0.623896 ms 增至 7.91991 ms。VTK 并行时间则从 163.799 ms 增至 3349.36 ms。在当前测试条件下，OpenGL 实现相对 VTK 并行实现具有明显时间优势。论文中应同时报告系统总时间和 GPU 计算时间，其中系统总时间更适合作为真实使用成本。

## 5.5 数据优化实验

数据优化实验在 ShipHull_0 数据集上进行。实验构造干净场，并叠加高斯扰动作为局部随机高频数值扰动的代理模型。随后运行数据优化模块，比较输入数据平均相对误差和优化后数据平均相对误差。

{md_table(["关联方式", "输入数据平均相对误差", "优化后数据平均相对误差", "改进比"], OPTIMIZATION)}

结果显示，POINT 关联方式下，平均相对误差由 0.296642 降至 0.180858，改进比为 0.610772；CELL 关联方式下，平均相对误差由 0.294558 降至 0.121921，改进比为 0.414806。两类字段优化后误差均低于输入误差，说明该模块对目标扰动具有稳定抑制效果。

## 5.6 实验结论

综合四组实验，可以得到以下结论。第一，解析场验证表明当前梯度算法主线具有正确性支撑。第二，真实字段与 VTK 对比表明系统输出具备工程一致性。第三，时间实验表明 OpenGL 实现具有明显并行效率优势。第四，高斯扰动优化实验表明数据优化模块能够降低局部随机高频扰动造成的误差。

同时，实验结论也应保持边界。本文不声称系统能够替代完整 VTK 或 ParaView，也不声称数据优化模块能处理所有类型噪声。当前结论只针对本文数据集、字段、硬件和测试条件成立。这样的表述更严谨，也更符合本科毕业论文答辩要求。
""",
    "06_总结与展望.md": """# 第六章 总结与展望

## 6.1 工作总结

本文围绕 CAE 后处理阶段的数据预处理问题，设计并实现了一套基于 OpenGL 的原型系统。系统以 VTK 数据读写为基础，以统一内部数据结构为核心，以 OpenGL Compute Shader 为计算执行手段，实现了规则网格梯度计算、非结构化网格梯度计算和面向局部随机高频扰动的数据优化。

在梯度计算方面，本文将技术路线收敛为规则网格有限差分法和非结构化网格形函数导数法。规则网格路径利用网格逻辑维度和相邻采样点计算差分，非结构化网格路径利用单元形函数导数和几何映射计算梯度。该口径清晰、与当前实现一致，也便于解释实验结果。

在数据优化方面，本文没有把模块泛化为通用降噪器，而是明确面向一类局部随机高频数值扰动。实验中使用高斯扰动作为代理模型，在 ShipHull_0 数据集上比较优化前后的平均相对误差。点数据和单元数据实验均表明优化后误差降低，说明该模块在目标场景下具有有效性。

在实验验证方面，本文按照解析场验证、VTK 一致性对比、VTK 时间对比和数据优化实验四条线展开。解析场验证结果达到 1e-7 量级，说明梯度算法实现具有正确性支撑；真实字段对比结果达到 1e-7 到 1e-6 量级，说明系统与 VTK 工程输出保持较高一致性；时间实验表明系统总时间和 GPU 计算时间均低于当前测试条件下的 VTK 并行时间；数据优化实验表明目标扰动下误差降低。

## 6.2 本文特点

本文的第一个特点是系统链路完整。项目不是单独实现一个公式或一个界面，而是形成了从 VTK 数据输入、内部数据转换、GPU 计算、结果写回到 VTK 导出的闭环。这种闭环能够支撑答辩时的功能演示和实验复现。

第二个特点是实验口径清晰。解析场验证、VTK 一致性和时间对比承担不同论证任务。解析场验证证明算法正确性，VTK 一致性证明工程输出可靠，时间对比证明实现效率。将三者区分开，可以避免论文中常见的“用一个实验解释所有问题”的逻辑缺陷。

第三个特点是结论边界明确。本文没有把系统描述为完整 CAE 后处理平台，也没有把数据优化模块描述为通用噪声处理器，而是严格围绕当前实现和实验数据展开。这种收敛后的表述更符合本科毕业设计的实际完成情况。

## 6.3 不足之处

当前系统仍然是原型系统，存在一些不足。首先，非结构化网格单元类型支持范围有限，目前主要覆盖一阶三角形、四边形、四面体和六面体。工程数据中可能存在更多高阶单元或混合单元，后续仍需扩展。

其次，数据优化实验使用高斯扰动作为代理模型，能够说明模块对一类局部随机高频扰动有效，但不能覆盖所有真实 CAE 扰动来源。真实工程数据中的误差可能与求解器、网格质量、边界条件和材料模型有关，需要更丰富实验进一步验证。

第三，系统 GUI 目前主要服务于基础演示和结果导出，还不具备完整可视分析软件的交互能力。若要发展为更完整工具，还需要增加渲染控制、字段管理、参数调节、批处理配置和异常提示等功能。

## 6.4 后续展望

后续工作可以从四个方面展开。第一，扩展更多单元类型和更复杂字段类型，使系统能够处理更广泛的工程数据。第二，完善数据优化实验，引入更接近真实 CAE 误差来源的扰动模型和评价指标。第三，增加自动化测试和批处理能力，使实验结果更容易复现。第四，将预处理模块与后续可视化模块更紧密结合，形成更完整的后处理数据准备流程。

总体来看，本文已经完成了本科毕业设计所需的核心系统、实验数据和论文论证链条。后续工作更多属于工程增强和适用范围扩展，而不是对当前主线的重新设计。
""",
    "07_参考文献.md": """# 参考文献

1. Khronos OpenGL Wiki. Compute Shader. https://www.khronos.org/opengl/wiki/Compute_Shader
2. Khronos OpenGL Wiki. Shader Storage Buffer Object. https://www.khronos.org/opengl/wiki/Shader_Storage_Buffer_Object
3. VTK documentation. vtkGradientFilter Class Reference. https://vtk.org/doc/nightly/html/classvtkGradientFilter.html
4. ParaView User's Guide. Understanding Data. https://docs.paraview.org/en/v5.10.0/UsersGuide/understandingData.html
5. Qt Documentation. Qt Widgets. https://doc.qt.io/qt-5/qtwidgets-index.html
6. Schroeder W., Martin K., Lorensen B. The Visualization Toolkit.
7. Ahrens J., Geveci B., Law C. ParaView: An End-User Tool for Large Data Visualization.
8. Owens J. D. et al. A Survey of General-Purpose Computation on Graphics Hardware. Computer Graphics Forum, 2007.
9. Hughes T. J. R. The Finite Element Method: Linear Static and Dynamic Finite Element Analysis.
10. Zienkiewicz O. C., Zhu J. Z. The superconvergent patch recovery and a posteriori error estimates. IJNME, 1992.
11. Tomasi C., Manduchi R. Bilateral Filtering for Gray and Color Images. ICCV, 1998.
12. Fleishman S., Drori I., Cohen-Or D. Bilateral Mesh Denoising. ACM TOG, 2003.
13. Perona P., Malik J. Scale-space and edge detection using anisotropic diffusion. IEEE TPAMI, 1990.
14. 教育部. 普通高等学校本科毕业论文（设计）抽检办法（试行）.
15. GB/T 7713.1-2025 信息与文献 编写规则 第1部分：学位论文.
16. GB/T 7714-2015 信息与文献 参考文献著录规则.
17. 国内 CAE 后处理与科学可视化应用研究.
18. 国内基于 GPU 的有限元或 CAE 后处理可视化研究.
19. 国内有限元方法与后处理教材、论文.
20. 国内图像、点云、网格和科学数据保边平滑研究.
""",
}


def main() -> None:
    OUT.mkdir(parents=True, exist_ok=True)
    for name, content in FILES.items():
        if FORBIDDEN.search(content):
            raise ValueError(f"{name} contains forbidden deprecated method wording")
        (OUT / name).write_text(content.strip() + "\n", encoding="utf-8")
    (OUT / "README.md").write_text(
        "# wendang 章节文件说明\n\n"
        "本文件夹按章节拆分论文 Markdown。正文口径以 `毕设进展.docx` 为准：规则网格有限差分，非结构化网格形函数导数，数据优化使用高斯扰动代理局部随机高频扰动。\n",
        encoding="utf-8",
    )
    print(f"generated {len(FILES) + 1} files in {OUT}")


if __name__ == "__main__":
    main()
